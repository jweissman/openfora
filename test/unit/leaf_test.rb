require 'test_helper'

class LeafTest < ActiveSupport::TestCase
  test "the truth" do
    assert true
  end

  # test "should be unique by name" do
  # 	Leaf.create :name => 'a'
  # 	# assert_fails(Leaf.create({:name => 'a'}), "Failure message.")
  # 	assert_throws StandardError.new do
  # 		Leaf.create({:name => 'b'}
  # 	end
  # end
end